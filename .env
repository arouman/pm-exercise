# Default configuration for the SubBase take-home starter.
# Committed to the repo on purpose: SQLite has no secrets and the defaults are dev-only.
# To override, edit this file — git won't fight you.

PORT=3000
DATABASE_URL="file:./dev.db"
